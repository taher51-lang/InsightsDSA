---
- name: Reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart insightsdsa uwsgi
  ansible.builtin.systemd:
    name: "{{ insightsdsa_systemd_unit }}"
    state: restarted

- name: Reload nginx
  ansible.builtin.service:
    name: nginx
    state: reloaded
